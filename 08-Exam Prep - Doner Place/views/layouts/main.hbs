<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Doner Place</title>
    <link href="/css/site.css" type="text/css" rel="stylesheet">
</head>

<body>
    <div class="container">
        <header>
            <span class="logo">Doner Place</span>

            <span class="greeting">
                {{#if currentUser}} Welcome, {{currentUser.username}}! {{else}} Welcome! {{/if}}
            </span>

            <ul class="menu">
                <li>
                    <a class="button" href="/">Home</a>
                </li>

                {{#if isAdmin}}
                <li>
                    <a class="button" href="/products/create">Create Product</a>
                </li>
                <li>
                    <a class="button" href="/orders/all">All Orders</a>
                </li>
                {{/if}}
                
                 {{#if currentUser}}
                <li>
                    <a class="button" href="/orders/status">My Orders Status</a>
                </li>
                <li>
                    <form id="logout-form" action="/users/logout" method="POST"> </form>
                    <a class="button" href="javascript:document.getElementById('logout-form').submit()">Logout</a>
                </li>
                {{else}}
                <li>
                    <a class="button" href="/users/register">Register</a>
                </li>
                <li>
                    <a class="button" href="/users/login">Login</a>
                </li>
                {{/if}}
            </ul>
        </header>

        <main>
            <div class="error">
                {{#if globalError}}
                <h3>{{globalError}}</h3>
                {{/if}}
            </div>

            {{{body}}}
        </main>

        <footer>Doner Place &copy; 2018</footer>
    </div>
</body>

</html>